# PSZ4RZ5610SUSYV37 source boundary

V37 is a quality-focused successor to V36. It retains the same 20 chiral
superfields and Pati--Salam gauge sector, while adjoining a pairwise-finite
spectator `Z85` to the V36 `Z66`. Because `gcd(66,85)=1`, their faithful cyclic
presentation is `Z5610`:

`q5610 = 85 q66 + 66 h85 (mod 5610)`.

Only the anomalons carry `Z85` charge:

- `h85(A2,A32)=(1,-1)`;
- `h85(A15,A17)=(69,-69)`;
- `h85(A16)=0`.

The linear and cubic `Z85` sums cancel pairwise. The choice `69^2=1 mod 85`
also cancels the tested mixed `Z4R-Z85^2` coefficient. The exact combined
`Spin x Z5610` Hsieh/Dai--Freed congruences pass.

The V36 terms `Pbar*A17^2` and `A16*A17` are removed because they are not
needed for mass rank. The retained terms

`Pbar*A2*A32 + P*A15*A17 + P*A16^2/2`

give `det(M_A)=a^2*b^2*c`. With the optimized accidental-PQ basis, exhaustive
state-space searches put the first superpotential PQ-breaking operator at
degree 33 and the first conservative Kahler monomial at degree 32.
An additional search over the exact charges of all 20 chiral species, before
imposing Pati--Salam index contractions, finds no lower-degree loophole.  The
singlet operators provide gauge-invariant witnesses that attain both bounds.

This source does not claim that G1 or G5 is fully closed. The mixed
Pati--Salam-selector anomaly, the complete product bordism with `Z4R`, the
origin of the large finite spectator, stable spectator-charged relics, Kahler
coefficients, soft mediation, global vacuum, and cosmology remain explicit
boundaries.
