# Exact global degree-eight conductor identity

Status: `EXACT_GLOBAL_PHI_ZERO_DEGREE8_CONDUCTOR__ORBIT_RIGIDITY_SEPARATE`.

For a real or complex four-form `Phi`, define

```text
N     = ||Phi||^2,
B     = *(Phi wedge Phi),
delta = 9 N^2 - 5 ||B||^2,
D     = delta/5.
```

The frozen exact certificate proves that `delta^2=25D^2` belongs to the
degree-eight invariant ideal generated by the quadratic residual covariants
`q54` and `q4125`.  Consequently,

```text
q54(Phi)=q4125(Phi)=0  =>  D(Phi)=0.
```

## Exact proof

The separately frozen plethysm/Littlewood certificate proves

```text
dim Sym^8(Lambda^4(C^10))^O(10) = 117.
```

There are 115 selected trace contractions

```text
tr(q_r q_a q_b q_c),   r in {54,4125},
```

and every one contains a residual factor.  Add the two invariants
`N^2 delta` and `N^4`.  Their evaluation matrix on 117 deterministic integer
four-forms has rank 117 modulo an exact prime, so these 117 invariants form a
basis of the entire orientation-even degree-eight invariant space.

The stored 117-by-118 integer table was reconstructed entrywise with eight
primes.  Their product is

```text
100009600393140930631089938244597828753987494496622481617,
```

while every centered table entry is bounded in absolute value by

```text
7985131778138331418532388852637630464000000.
```

The bound follows from orthogonality of the pair-Casimir projectors,
`||q_a||_F <= N`, together with the fixed projector denominators and a direct
integer bound for `B`.  The CRT modulus exceeds twice the entry bound, so the
centered entries are uniquely reconstructed over the integers.

After exact row and column gcd removal, SymPy's fraction-free
`DomainMatrix.solve_den(..., method="rref")` gives a 1752-digit common
denominator.  The exact residual is zero in every row and the final two
solution numerators, belonging to `N^2 delta` and `N^4`, are exactly

```text
[0, 0].
```

Thus `delta^2` is a rational combination of only the 115 residual traces.
Dividing by 25 gives the claimed `D^2` identity.

## Frozen data

- evaluation table SHA-256:
  `beab3649ca03c3ee3c6fc2ab700efedfe614328a6da52f2234d3b9610f3c167c`;
- exact solution SHA-256:
  `c49833b4f90b0b5a6604d4d5aded36ea00944dd198d2ceefd8def8213174dcfa`;
- certificate core SHA-256:
  `3763506628c0aac91fc54fdd1b49f6cdb12114707a13f2359ba3acc2b4836142`.

The source can recompute any number of the eight live modular tables.  The
stored scratch reconstruction source reproduces the complete CRT table from
the deterministic seed.

## Fail-closed boundary

This artifact proves the conductor identity and `D=0` on the common residual
zero locus.  It does not by itself prove the signed-Kaehler orbit rigidity,
an orbit-distance inequality, or the final G3/G4 positivity statement.
