# RG / threshold / FCNC re-audit findings — v20

The newest completion claims were re-audited fail-closed.

## Findings

1. The proposed two-loop H/F terms are a compressed heuristic ansatz, not a term-by-term specialization of a complete published two-loop system.
2. The interval from `M_I` to `M_GUT` is a broken/intermediate-group interval, but the implementation used an SO(10)-style beta and the GUT coupling as its anchor.
3. The common-scale targets were constructed from diagnostic singular-value ratios and clipped to `[0.05, 5]`; they are not a precision high-scale mass data set.
4. The FCNC ledger converts matrix norms into illustrative coupling scales; it does not compute channel amplitudes, form factors, branching ratios, acceptances, or experimental likelihoods.

## Correct classification

- portal and hierarchy calculations: useful diagnostics;
- broken-phase matrix ODE: diagnostic only;
- precision common-scale fit: open;
- Pati-Salam matching: open;
- full two-loop SO(10)+210 Yukawa closure: open;
- finite-model FCNC likelihood: open;
- internal candidate and conditional benchmark: still eligible for the ultimate gate;
- full phenomenology and discovery: blocked.
